apiVersion: v1
kind: Service
metadata:
  name: llmpostor
  labels:
    app.kubernetes.io/name: llmpostor
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: llmpostor
